# Copy this file to `.env` and fill in from your Supabase project
# (Dashboard -> Project Settings -> API). Without a .env the app runs
# in DEMO mode: data is stored only in the browser (localStorage).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
